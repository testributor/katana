- if @project.tracked_branches.empty?
  %h2 No branches are tracked! None. Zero. Zilch. Nada.
  Please
  = link_to 'add', new_project_branch_path(@project)
  at least one branch to this project.
- else
  %h2= "Your tracked branches in project '#{@project.repository_name}'"
  %h3 Select which branch you want to manage
  %ol
    - @project.tracked_branches.each do |b|
      %li= link_to b.branch_name, project_branch_test_jobs_path(@project, b.id)
  %p
    or
    = link_to 'add', new_project_branch_path(@project)
    one more branch to this project.
