- breadcrumb :tracked_branch, current_project, @tracked_branch

- content_for :page_title do
  = @tracked_branch.branch_name

.panel.panel-default
  .panel-body
    .row
      .col-md-12.col-sm-12.col-xs-12
        .pull-right
          = link_to new_project_branch_test_run_path, method: :post,
            action: :create do
            %button.btn.btn-icon.btn-info.w-md
              %i.fa.fa-plus.m-r-5
              %span= 'Add a new run'
          = link_to "Destroy branch",
            project_branch_path(current_project, @tracked_branch),
            method: :delete, class: 'btn btn-danger',
            data: { confirm: "Branch will be removed from this project. Are you sure?" }

        %table.table
          %thead
            %tr
              %th= "Test Run"
              %th= "Commit message (SHA)"
              %th= "Status"
              %th= "Total run time"
              %th= "Actions"
          %tbody
            - @tracked_branch.test_runs.order("created_at DESC").each do |test_run|
              %tr
                %td.col-md-1= link_to "##{test_run.id}",
                  project_branch_test_run_path(current_project, test_run.tracked_branch_id, test_run)
                %td.col-md-4
                  = test_run.decorate.commit_message
                  %br
                  %i= test_run.decorate.commit_info
                %td.col-md-2
                  .test{class: test_run.status.css_class}
                    - if test_run.status.code == TestStatus::RUNNING
                      %i.fa.fa-cog.fa-spin.fa-1x.fa-fw.m-r-5{style: "color: #{test_run.decorate.cog_color}" }
                    %span
                      = test_run.status.text
                %td.col-md-2
                  - if test_run.total_running_time
                    = distance_of_time_in_words(0, test_run.total_running_time, include_seconds: true)
                %td.col-md-3
                  = render partial: 'test_run_actions', locals: { test_run: test_run }
