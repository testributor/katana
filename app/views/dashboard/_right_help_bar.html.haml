.row.panelized
  .col-md-12
    %h4
      %i.fa.fa-cogs
      Workers Running (UUID)

    .m-t-10
      - current_project.active_workers.each do |w|
        %div
          %i.fa.fa-cog.fa-spin.fa-1x{style: 'color: green;'}
          = w.split("_").last[0..7]
    %br
    %p
      You can
      = link_to worker_setup_project_settings_path(current_project) do
        %b configure
      your workers here.
    %hr
  .col-md-12
    %h4
      %i.fa.fa-book
      = link_to 'Documentation', 'http://documentation.testributor.com', target: '_blank'

    Documentation is under continuous improvement.
    %br
    We do our best to keep it up to date and complete but don’t hesitate to contact us if you find something missing or wrong.
    %hr
  .col-md-12
    %h4
      %i.fa.fa-life-ring
      Help

    %p
      We are always available to answer any question.

    %p
      = link_to gitter_share_link, class: 'margin-top: 2px;', target: "_blank" do
        %span.m-r-5
          Ask us anything or send us some feedback!
        = image_tag("https://badges.gitter.im/owner/repo.png", style: "vertical-align: text-top;")
    %p
      Or send us an e-mail at
      = mail_to 'support@testributor.com', 'support@testributor.com'
