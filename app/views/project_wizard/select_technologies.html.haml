.panel-body
  %h3 Select your services by clicking on the input below:
  = form_for @project_wizard, url: project_wizard_path,
    method: :put, html: { class: 'form-horizontal' } do |f|
    = select_tag :selected_technologies,
      options_for_select(ProjectWizard::AVAILABLE_TECHNOLOGIES,
      selected: @project_wizard.selected_technologies),
      multiple: true, class: 'multi-select'
    .pull-right.m-t-15
      = f.submit "Create Project", class: 'btn btn-primary w-lg btn-lg'
